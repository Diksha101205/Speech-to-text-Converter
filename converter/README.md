# Speech-to-Text Converter Frontend

React + Vite frontend for uploading or recording audio and displaying transcription results from the Express backend.

```bash
npm run dev
```

The Vite dev server proxies `/api` requests to `http://localhost:5000`.
